"use strict"; //treat all the JS code as a newer JS code

// priceValue = 1090;
// console.log(priceValue);

/* This code will not run because of strict mode if it does not use of it, priceValue variable becomes a global variable and code runs.
 */

// alert("This is JS learning curve"); I am nodejs not browser

console.log(10 + 20); //code readability should be high
console.log("Asif");

let name = "Asif Khan";
let age = 20;
let isLoggedIn = false;
 
//number => 2 to power 53(Range)
//bigInt
//strin =>""
//boolean => true / false
//null => dataType+standalone value =>(it's object type)
//undefined
//symbol => unique

//object

console.log(typeof undefined); // It's type is undefined 
console.log(typeof null); // It's type is object 
